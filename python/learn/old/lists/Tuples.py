# änlich wie listen
# Unterschiede:
# - kann im nachhinein nicht verendert werden
# - hat eienen anderen syntax
#
coordinates = [(4, 5), ("LOL"), (6.509)]          # syntax unterschied: Listen mit mehreren Daten haben ()
print(coordinates)
# coordinates[1] = 10         #funktioniert nicht
print(coordinates[1])   # druckt Daten von Stelle x oder so
