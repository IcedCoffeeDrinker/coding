# mit return geben Funktionen Informationen aus, wie z.B. ob alles richtig gelaufen ist oder das ergebniss einer Recknung
# wehe du fegisst das!

def qubik (num):
    return num * num * num          # "return" gibt das Ergebniss der Rechnung so aus, dass man die Funktion drucken kann
                                    # unter "return" kann nichts mehr stehen
print(qubik(2))                     # speichert das Ergebniss und gibt gleichzeitig einen Input

ergebniss = qubik(4)                 # speichert die rechnung

