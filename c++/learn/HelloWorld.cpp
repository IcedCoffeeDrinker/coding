#include <iostream>     // confugration ??

using namespace std;

int main()      // int = function; main will always run once at the beginning
{               // note syntax!
    cout << "Hello world!" << endl;     // cout = console-out (print); endl = end-line (next line)
    cout << "Hello world!";     //works without too
    cout << "Hello world!";
    return 0;     // program works without i guess
}
